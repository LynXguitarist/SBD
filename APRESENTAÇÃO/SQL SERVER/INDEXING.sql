-- 1 -> comparar todos os indices iguais no Oracle vs SQL Server
	-- Clustered, NonClustered, hash

-- 2 -> ver tambem indices diferentes
	-- SQL Server -> 1computed columns, 2filtered (parecido ao bitmap), full text, columnstore
	-- Oracle -> 1Index Based on Arithmetic Expression, 2bitmap